{
  "version": 2,
  "regions": ["iad1"],
  "builds": [
    {
      "src": "src/server.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "src/server.ts" },
    { "src": "/(.*)", "dest": "src/server.js" }
  ]
}
